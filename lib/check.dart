import 'package:flutter/cupertino.dart';

class CheckCommitIssue extends StatelessWidget {
  const CheckCommitIssue({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
